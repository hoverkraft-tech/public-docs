---
sidebar_position: 2
---

# Projects

import { HoverkraftProjectCard } from '@hoverkraft/docusaurus-theme/components';
import styles from './projects.module.css';

Welcome to the Hoverkraft project ecosystem! We maintain **<%= stats.totalRepos %> open-source projects**
with a combined **<%= stats.totalStars %> stars** across multiple technologies
including <%= stats.languagesSummary %>, and more. 

## 📊 Quick Stats

- **<%= stats.totalRepos %>** Active Projects
- **<%= stats.totalStars %>** GitHub Stars
- **<%= stats.languageCount %>** Programming Languages
- **100%** Open Source

<% categories.forEach((category) => { %>
    
## <%= category.name %>

<div className={styles.projectsGrid}>
  <% category.repositories.forEach((repository) => { %>
  <HoverkraftProjectCard
    icon="<%= repository.icon %>"
    title="<%= repository.name %>"
    titleHref="<%= repository.htmlUrl %>"
    meta="<%= repository.language %> • ⭐ <%= repository.stars %> • Updated <%= repository.lastUpdated %>"
    description="<%= repository.description %>"
    tags={[<% repository.topics.forEach((topic, index) => { %>"<%= topic %>"<%= index < repository.topics.length - 1 ? ', ' : '' %><% }) %>]}
    actions={[
      { label: "View on GitHub", href: "<%= repository.htmlUrl %>" }<% if (repository.homepage) { %>,
      { label: "Documentation", href: "<%= repository.homepage %>" }<% } %>
    ]}
  />
  <% }) %>
</div>

<% }) %>

---
*This page is automatically generated from our GitHub repositories.
Last updated: <%= stats.generatedDate %>*
